# Persist user-created activities

Right now, dynamically added hangouts disappear when the page is reloaded. We
should add backend support (or local storage) to save new activities so that
they're available to other users and survive restarts.
