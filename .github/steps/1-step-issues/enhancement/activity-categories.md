# Support categorized activities with custom icons

Introduce a `type` or `category` field for each activity (e.g. "Academic",
"Sports", "Nightlife", "Misc") and use a different marker icon for each on
the map. This will help users quickly distinguish the kinds of events.
