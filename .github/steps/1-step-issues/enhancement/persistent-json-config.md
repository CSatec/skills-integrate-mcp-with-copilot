# Provide persistent JSON configuration storage

Introduce a JSON file that persists user configuration and history (e.g. previously entered contact info or paragraph selections). The system should read/write to this file automatically.

**Motivation:**
- Avoid retyping data on every run
- Maintain a record similar to `sample.json` in the generator project
