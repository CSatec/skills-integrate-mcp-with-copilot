# Add place search for activity locations

Users should be able to pick a location when creating an activity. A search
box powered by the Google Places API (or similar) would let them type an
address/place and have the map center on it. The selected coordinates should be
stored with the activity.
