# Add text-formatting helper utilities

Create shared helper functions for formatting text (alignment, indentation, font sizing) when generating documents or rich text output. These utilities will abstract repetitive styling code and keep generation logic clean.

**Examples:**
- `format_alignment(paragraph_obj, indent)`
- `format_fill_in_info(template_string, data)`
