# Add map-based interface

Our current app shows a static list of activities. It would be great to integrate
Google Maps (or another mapping service) so users can see activities pinned to
real-world locations. This would require:

- Initializing a map centered on the user's geolocation.
- Showing markers for each activity with custom icons.
- Allowing users to pan/zoom and search for addresses.
