# Let users create activities directly from the map

Build a form on the front end that lets people submit a new "hangout" or
activity. When submitted, the event should appear immediately as a marker on the
map (with its metadata). Currently our activities are hardcoded and cannot be
added at runtime.
